#ddev-generated
webimage_extra_packages: [build-essential]
hooks:
  post-start:
    - exec: sudo ln -sf /usr/local/src/static-php-cli/buildroot/bin/php /usr/bin/php
    - exec: sudo ln -sf /usr/local/src/static-php-cli/buildroot/bin/php-fpm /usr/sbin/php-fpm
    - exec: sudo ln -sf /etc/php/${PHP_PATCH_VERSION%.*}/cli/php.ini /etc/php.ini
    - exec: sudo ln -sf /etc/php/${PHP_PATCH_VERSION%.*}/fpm/php-fpm.conf /etc/php-fpm.conf
    - exec: sudo killall -HUP supervisord
